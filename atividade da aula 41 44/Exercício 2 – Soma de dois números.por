programa
{
    funcao inicio()
    {
        inteiro a, b, soma
        escreva("Digite A: ")
        leia(a)
        escreva("Digite B: ")
        leia(b)
        
        soma = a + b



        escreva("Soma = ", soma)
        // Erro do código: O código estava informndo que iria realizar uma soma, porém o sinal de multiplicação (*) estava localizado do lugar onde iria ser realizada a soma(+)
        // Solução do código: trocar so sinal de multiplicação pelo sinal da soma. 
    }
}